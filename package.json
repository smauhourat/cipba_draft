{
  "name": "cipba-static-build",
  "private": true,
  "version": "1.0.0",
  "description": "Genera dist/ : version estatica desplegable del prototipo en project/",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
